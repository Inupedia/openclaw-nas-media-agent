# Agent Quick Deployment Documentation Red-Test Record

The contract tests intentionally precede the documentation implementation.

Expected failures before implementation:

- `README.md` does not yet contain `## 快速部署：复制给 Agent`.
- `README.md` and `docs/deployment/QUICKSTART.md` do not yet contain canonical prompt markers.
- root `AGENTS.md` does not yet exist.

The implementation must make these tests pass without changing deployment runtime behavior.
